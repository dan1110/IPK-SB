import '../../design-system/tokens.css'

export const metadata = {
  title: 'Design System Preview — Project Knowledge Workspace',
}

export default function DemoLayout({ children }: { children: React.ReactNode }) {
  return children
}

version: '3.8'
services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - ZAPIER_WEBHOOK_SECRET=${ZAPIER_WEBHOOK_SECRET}
